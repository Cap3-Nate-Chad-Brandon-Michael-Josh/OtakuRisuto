// a List Container that returns when searched. For example, if a user searches "basic bitch" it might be a list containing "Dragonball z. sword art online, naruto, ect"
import React from 'react';
import KitsuApiService from '../../services/kitsuApiServices';

class SearchListItem extends React.Component {
    state = {
        error: null,
    }

    componentDidMount() {

    }    

    render() {
        return (
            <div>

            </div>
        )
    }
}

export default SearchListItem;